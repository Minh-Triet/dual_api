apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  ports:
    - name: port1
      port: 5000
      protocol: TCP
      targetPort: 5000
    - name: port2
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: my-app